fixtures:
  repositories:
    stdlib:
      repo: 'git://github.com/puppetlabs/puppetlabs-stdlib.git'
      ref: '4.1.0'
      flags: --single-branch
    nexus_rest:
      repo: 'https://bitbucket.org/atlassian/puppet-module-nexus_rest'
      ref: 'v0.6.0'
      flags: --single-branch
  symlinks:
    nexus_proxy: "#{source_dir}"
